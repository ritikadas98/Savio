import { BottomNav } from '../components/layout/BottomNav';

export function ReflectPage() {
  return (
    <div className="min-h-screen bg-canvas pb-24 flex flex-col items-center justify-center p-6 text-center">
      <h1 className="text-title font-medium mb-2">Reflect</h1>
      <p className="text-secondary">Phase 3 — coming next</p>
      <BottomNav />
    </div>
  );
}
