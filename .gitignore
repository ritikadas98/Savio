# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# tsc -b project-build incremental cache (per-developer)
*.tsbuildinfo

# Supabase CLI working state — per-developer, not source
supabase/.temp/

# Claude Code session state — per-developer, not source
.claude/

# Local-only portfolio blurb (kept off the repo deliberately)
BLURB.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.gcp-sa.json